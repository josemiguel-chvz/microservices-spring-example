spring:
  application:
    name: eureka-service
  cloud:
    config:
      enabled: true
      fail-fast: true
      retry:
        maxAttempts: 200
        maxInterval: 10000
      uri: http://localhost:8081